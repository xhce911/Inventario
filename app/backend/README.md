# Backend

Base preparada para una API por dominios:

- `locations`: ubicaciones del aula.
- `inventory`: activos y componentes.
- `audits`: revisiones historicas.
- `users`: responsables y permisos.
- `reports`: exportaciones.

La implementacion real debe conectarse al esquema de `app/database/schema.sql`.
